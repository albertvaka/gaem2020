#pragma once

#include <array>

#include "vec.h"
#include "bounds.h"
#include "SDL_gpu.h"

struct TiledEntities
{
	static const std::array<vec, 1383> obstacle;
	static const vec spawn;
	
};

struct TiledAreas
{
	
};